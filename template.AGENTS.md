# AGENTS.md Template

## Project Overview
Briefly describe the purpose and scope of this repository/project for coding agents.

## Dev environment tips
- List commands, environment variables, or setup steps agents should know.
- Example: Use `npm run dev` for hot reload, not `npm run build`.

## Testing instructions
- How to run tests, what to check, and any CI requirements.
- Example: Run `npm test` before submitting changes.

## PR instructions
- Preferred PR title format, review requirements, and merge rules.
- Example: Title format `[module] Short description`, always run lint/tests before PR.

## Agent-specific guidance
- Any conventions, file locations, or workflows unique to this repo that agents should follow.
- Example: Store new utilities in `/src/utils/`, update `/docs/` for new features.

---

_This template follows the [AGENTS.md](https://agents.md) open format. Add, remove, or reorder sections as needed for your project._
